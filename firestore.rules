rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Solo para pruebas internas mientras agregas autenticacion.
    // En produccion no dejes esta regla abierta.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
